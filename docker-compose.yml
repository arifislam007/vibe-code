version: '3.8'

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - DB_PATH=/data/db.json
    volumes:
      - db-data:/data
    restart: unless-stopped

volumes:
  db-data:
    driver: local
